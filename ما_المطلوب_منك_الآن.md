# 🎯 ما المطلوب منك الآن - خطوات بسيطة

## ✅ الوضع الحالي:
- ✅ الكود جاهز 100%
- ✅ V1 API مفعّل في Firebase
- ✅ Admin Panel جاهز لإرسال Push Notifications
- ⏳ **ينقص فقط: Access Token**

---

## 🚀 المطلوب منك (3 خطوات رئيسية):

### 📍 الخطوة 1: إنشاء OAuth Client ID

#### 1.1 افتح Google Cloud Console:
```
https://console.cloud.google.com/apis/credentials?project=pharmanow-754a7
```

#### 1.2 أنشئ OAuth Client:
1. **اضغط "+ CREATE CREDENTIALS"** (في الأعلى)
2. **اختر "OAuth client ID"**
3. **Application type:** اختر **"Web application"**
4. **Name:** اكتب **"FCM Push Notifications"**
5. **Authorized redirect URIs:** اضغط **"+ ADD URI"** واكتب:
   ```
   https://developers.google.com/oauthplayground
   ```
6. **اضغط "CREATE"**

#### 1.3 انسخ البيانات:
بعد الإنشاء، ستظهر نافذة بها:
- ✅ **Client ID** (انسخه)
- ✅ **Client secret** (انسخه)

**احتفظ بهما! ستحتاجهما في الخطوة التالية**

---

### 📍 الخطوة 2: الحصول على Access Token

#### 2.1 افتح OAuth Playground:
```
https://developers.google.com/oauthplayground/
```

#### 2.2 إعداد OAuth Playground:
1. **اضغط على أيقونة الإعدادات (⚙️)** في الأعلى اليمين
2. **فعّل ✅ "Use your own OAuth credentials"**
3. **الصق:**
   - **OAuth Client ID:** (من الخطوة 1.3)
   - **OAuth Client secret:** (من الخطوة 1.3)
4. **اضغط "Close"**

#### 2.3 Authorize API:
1. **في الجانب الأيسر (Step 1):**
   - ابحث عن: **"Firebase Cloud Messaging API v1"**
   - أو الصق: `https://www.googleapis.com/auth/firebase.messaging`
   - **فعّله ✅**
2. **اضغط "Authorize APIs"** (الزر الأزرق)
3. **اختر حساب Google** (نفس الحساب المرتبط بـ Firebase)
4. **اضغط "Allow"** (السماح)

#### 2.4 احصل على Access Token:
1. **في Step 2 (Exchange authorization code for tokens):**
2. **اضغط "Exchange authorization code for tokens"**
3. **انسخ "Access token"** ✅ (النص الطويل)

**احتفظ به! ستحتاجه في الخطوة التالية**

---

### 📍 الخطوة 3: حفظ Access Token في Firestore

#### 3.1 افتح Firestore:
```
https://console.firebase.google.com/project/pharmanow-754a7/firestore
```

#### 3.2 أنشئ/عدّل Document:

##### إذا كان `system_settings` موجود:
1. **اضغط على `system_settings`**
2. **ابحث عن `fcm_config`:**
   - **إذا موجود:** اضغط عليه → Edit → عدّل `accessToken`
   - **إذا غير موجود:** اضغط **"+ Add document"**

##### إذا لم يكن `system_settings` موجود:
1. **اضغط "Start collection"**
2. **Collection ID:** اكتب `system_settings`
3. **اضغط "Next"**

#### 3.3 أدخل البيانات:
```
Document ID: fcm_config

Field:
- Name: accessToken
- Type: string
- Value: [الصق Access Token من الخطوة 2.4]
```

#### 3.4 احفظ:
**اضغط "Save"** ✅

---

## 🎉 اختبار النظام:

### 1. افتح Admin Panel:
```
http://localhost:5173/admin/notifications
```

### 2. تحقق من التبويبات:
- ✅ يجب أن يظهر تبويب **"Push (Mobile)"** مفعّل
- ✅ إذا كان معطل، أعد تحميل الصفحة (F5)

### 3. أرسل إشعار تجريبي:
1. **اختر تبويب "Push (Mobile)"**
2. **املأ:**
   - العنوان: "اختبار"
   - المحتوى: "هذا إشعار تجريبي"
   - المستهدفون: "الجميع"
3. **اضغط "إرسال Push Notification"**

### 4. النتيجة المتوقعة:
- ✅ رسالة نجاح: "تم إرسال X إشعار بنجاح"
- ✅ الإشعار يصل لجميع الأجهزة (Web + Android + iOS)

---

## ⚠️ ملاحظات مهمة:

### 1. Access Token ينتهي بعد ساعة:
- ⏰ صالح لمدة **ساعة واحدة فقط**
- 🔄 بعد ساعة، ارجع للخطوة 2.4 وجدده

### 2. لتجديد Access Token:
```
1. افتح OAuth Playground
2. في Step 2، اضغط "Refresh access token"
3. انسخ الـ Access Token الجديد
4. حدّثه في Firestore (system_settings/fcm_config/accessToken)
```

### 3. للاستخدام الدائم (اختياري):
- استخدم Service Account (يحتاج Backend)
- أو ترقية لـ Blaze Plan + Cloud Functions

---

## 📋 ملخص سريع:

```
✅ الخطوة 1: Google Cloud Console → Create OAuth Client ID
✅ الخطوة 2: OAuth Playground → Get Access Token
✅ الخطوة 3: Firestore → Save Access Token
✅ الخطوة 4: Admin Panel → Test Push Notification
```

---

## 🆘 إذا واجهت مشكلة:

### المشكلة: "Access Token غير موجود"
**الحل:** تأكد من حفظ Access Token في Firestore بالضبط:
```
system_settings > fcm_config > accessToken
```

### المشكلة: "لا توجد FCM tokens"
**الحل:** تأكد من أن المستخدمين سمحوا بالإشعارات في التطبيق

### المشكلة: "Failed to send"
**الحل:** 
1. تحقق من أن Access Token صحيح
2. تحقق من أنه لم ينتهي (أقل من ساعة)
3. جدده من OAuth Playground

---

## 🎯 الخلاصة:

**بعد هذه الخطوات الثلاث، ستتمكن من:**
- ✅ إرسال Push Notifications من Admin Panel
- ✅ الوصول لجميع المستخدمين (Web + Mobile)
- ✅ بدون Cloud Functions
- ✅ بدون Blaze Plan
- ✅ مجاناً تماماً!

**⏰ فقط تذكر: جدد Access Token كل ساعة**

---

**🚀 ابدأ الآن من الخطوة 1!**
