# ميزة تعديل الدواء قبل الموافقة - Admin Medicine Edit Feature

## التحديث
تم إضافة إمكانية تعديل الدواء للأدمن قبل الموافقة عليه في صفحة مراجعة الأدوية.

## الميزات المضافة

### 1. زر تعديل في كارت الدواء
- تم إضافة زر "تعديل" 🔵 بجانب أزرار "موافقة" و "رفض"
- الزر يظهر فقط للأدوية في حالة "قيد المراجعة" (pending)

### 2. Dialog التعديل الكامل
يحتوي على:
- **المعلومات الأساسية**: الاسم، الكود، الوصف، السعر، الكمية، الفئة، الشركة المصنعة
- **إدارة الصور**: 
  - رفع صورة من الجهاز
  - إضافة رابط صورة من الإنترنت
  - إزالة خلفية الصورة ✨
  - حذف الصورة 🗑️
  - معاينة الصورة
- **حذف الصور القديمة**: يتم حذف الصور القديمة من Supabase تلقائياً عند التعديل

### 3. التحديث في pending_medicines
- التعديلات تُحفظ مباشرة في `pending_medicines` collection
- الدواء يبقى في حالة "pending" بعد التعديل
- يمكن للأدمن الموافقة على الدواء بعد التعديل

## سير العمل (Workflow)

1. الأدمن يفتح صفحة "مراجعة الأدوية"
2. يرى الأدوية في حالة "قيد المراجعة"
3. إذا أراد تعديل دواء قبل الموافقة:
   - يضغط على زر "تعديل" 🔵
   - يفتح dialog التعديل
   - يعدل البيانات المطلوبة (الاسم، السعر، الصورة، إلخ)
   - يحفظ التعديلات 💾
4. بعد التعديل، يمكنه:
   - الموافقة على الدواء ✅
   - أو رفضه ❌
   - أو تعديله مرة أخرى 🔵

## الملفات المعدلة

### `src/pages/admin/AdminMedicineReview.tsx`
- إضافة imports جديدة للأيقونات والوظائف
- إضافة states للـ edit dialog
- إضافة دوال:
  - `handleOpenEdit()`: فتح dialog التعديل
  - `handleSubmitEdit()`: حفظ التعديلات
  - `handleImageUpload()`: رفع صورة جديدة
  - `handleRemoveBackground()`: إزالة خلفية الصورة
- إضافة زر "تعديل" في كارت الدواء
- إضافة Edit Dialog كامل مع جميع الحقول

## الأمان والتحسينات

✅ **حذف الصور القديمة**: يتم حذف الصور القديمة من Supabase تلقائياً لتوفير المساحة
✅ **إخفاء حقل الرابط**: يُخفى حقل رابط الصورة للصور المعالجة (من Supabase) لأسباب أمنية
✅ **منع إزالة الخلفية المتكررة**: زر إزالة الخلفية يُخفى بعد الاستخدام لتوفير API calls
✅ **معالجة الأخطاء**: رسائل خطأ واضحة للمستخدم
✅ **التحقق من البيانات**: التأكد من وجود صورة قبل الحفظ

## الاختبار

تم اختبار:
- ✅ فتح dialog التعديل
- ✅ تعديل جميع الحقول
- ✅ رفع صورة جديدة
- ✅ إزالة خلفية الصورة
- ✅ حذف الصورة
- ✅ حفظ التعديلات في Firebase
- ✅ البناء (build) بدون أخطاء

## ملاحظات

- التعديلات تُحفظ في `pending_medicines` collection فقط
- الدواء لا ينتقل إلى `medicines` collection إلا بعد الموافقة
- يمكن تعديل الدواء عدة مرات قبل الموافقة
- جميع ميزات إدارة الصور متاحة (رفع، حذف، إزالة خلفية)
